services:
  troviet-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: troviet-pro
    restart: unless-stopped
    ports:
      - "3000:4000"
    volumes:
      - troviet_data:/app/data
    environment:
      - PORT=4000
      - NODE_ENV=production

volumes:
  troviet_data:
    driver: local